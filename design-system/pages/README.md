# Page Overrides

Use this folder only when a page needs a justified deviation from `design-system/MASTER.md`.

Rules:
- keep overrides small;
- do not repeat global rules;
- one file per page;
- name files in lowercase, for example `task.md` or `onboarding.md`.
